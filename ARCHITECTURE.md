# معماری پروژه — بات تلگرامی حسابداری/انبار باتری‌فروشی

> این فایل «قرارداد» بین ماژول‌هاست. هر ماژول باید دقیقاً همین ورودی/خروجی را رعایت کند
> تا بشود آن‌ها را جدا از هم تست و دیباگ کرد. کد اینجا نیست — فقط نقشه و قرارداد.

---

## ۱. پشته فناوری (Tech Stack) — تصمیم نهایی

> **به‌روزرسانی مهم:** بعد از بررسی پروژه‌ی قبلی تو (تسویه‌یار)، تصمیم گرفتیم به‌جای دو سرویس جدا (بات Node.js + UI مدیریتی Next.js)، از همون معماری تک‌سرویسیِ Next.js پیروی کنیم که قبلاً باهاش کار کرده‌ای. دلیل در بخش ۱.۲ توضیح داده شده.

| بخش | انتخاب | دلیل |
|---|---|---|
| فریم‌ورک اصلی | **Next.js (App Router) — یک پروژه‌ی واحد** | هم Webhook تلگرام (API Route) هم UI مدیریتی در یک codebase؛ الگوی تسویه‌یار |
| میزبانی | **VPS شخصی (Docker Compose) یا Railway (همان Dockerfile)** | هر دو از یک Dockerfile استفاده می‌کنند؛ انتخاب بین‌شان فقط زمان دیپلوی است، نه تغییر کد (جزئیات: `RAILWAY.md` در پروژه) |
| دیتابیس/ORM | PostgreSQL + **Prisma** | JSONB برای فیلدهای داینامیک؛ همان ابزار آشنا از پروژه‌ی قبلی |
| رابط تلگرام | **Webhook** از طریق `app/api/bot/webhook/route.ts`، پشت Nginx + SSL | یک پورت، یک دامنه، هم برای UI هم برای Webhook |
| کلاینت تلگرام | **HTTP خام دست‌ساز** (بدون Telegraf) — طبق الگوی `telegramApi.ts` پروژه‌ی قبلی | سبک‌تر، کنترل کامل، بدون وابستگی اضافه |
| معماری مکالمه بات | **Dispatcher مستقل از پلتفرم** (طبق الگوی `dispatcher.ts`) | امکان افزودن بله یا هر پیام‌رسان دیگر بدون بازنویسی منطق |
| STT (صوت→متن) | Groq API (Whisper-large-v3) به‌صورت پیش‌فرض | رایگان، سریع |
| Vision + Parser | Google AI Studio (Gemini Flash) به‌صورت پیش‌فرض | رایگان، هم Vision هم استخراج JSON |
| لایه انتزاعی مدل AI | **AI Provider Adapter** با پشتیبانی از 9router/OpenRouter (بخش ۱.۱) | جابجایی مدل بدون تغییر کد سرویس‌ها |
| Fallback شبکه | متن ساده / ریپلای فرمت‌دار | صفر وابستگی به API |
| لاگ | JSON ساختاریافته با `request_id`، خروجی stdout | دیباگ روی VPS با `docker logs` |

> تغییر پشته فقط با ثبت دلیل در `ROADMAP.md → Decision Log` مجاز است.

### ۱.۱ لایه انتزاعی مدل AI (AI Provider Adapter)

چون گفتی می‌خوای بعداً بتونی از 9router یا OpenRouter برای مدل‌های دیگه استفاده کنی، ماژول‌های `stt/` و `vision/` هرگز مستقیم به Groq یا Gemini وصل نمی‌شوند. بین آن‌ها و سرویس واقعی یک لایه‌ی نازک آداپتور قرار می‌گیرد:

```
lib/stt/     ──► lib/providers/stt/groq.ts
             ──► lib/providers/stt/openrouter.ts   (در صورت نیاز فعال می‌شود)

lib/vision/  ──► lib/providers/vision/gemini.ts
             ──► lib/providers/vision/openrouter.ts
```

- هر provider یک فایل است که همان قرارداد ورودی/خروجی ماژول اصلی (بخش ۳) را پیاده می‌کند.
- انتخاب provider از طریق متغیر محیطی (`STT_PROVIDER=groq`, `VISION_PROVIDER=gemini`) مشخص می‌شود — نه در کد.
- اگر provider اول خطا داد/سهمیه‌اش تمام شد، می‌توان یک provider دوم را به‌عنوان زنجیره‌ی fallback در همان متغیر تعریف کرد (مثلاً `STT_PROVIDER=groq,openrouter`).
- تغییر یا اضافه کردن یک مدل جدید یعنی فقط یک فایل جدید در `providers/` — هیچ ماژول دیگری دست نمی‌خورد.

### ۱.۲ چرا از دو سرویس جدا به یک Next.js واحد برگشتیم

در نسخه‌ی اول این سند، یک بات Node.js/Telegraf جدا از یک UI مدیریتی Next.js پیشنهاد شده بود. بعد از بررسی پروژه‌ی تسویه‌یار (نسخه‌ی قبلی تو، که با موفقیت روی Railway اجرا شده)، این تصمیم برگشت خورد، چون:

- **کد تکراری کمتر:** در معماری دو-سرویسی، مدل‌های دیتابیس، اعتبارسنجی، و منطق کسب‌وکار باید بین دو codebase هماهنگ بمانند. در تک-سرویسی، همه از یک Prisma schema و یک‌سری توابع مشترک استفاده می‌کنند.
- **الگوی آماده و تست‌شده:** تسویه‌یار دقیقاً همین ساختار (Next.js API Route برای Webhook + صفحات مدیریتی در همان پروژه) را دارد و پایدار در تولید اجرا شده — پس ریسک کمتری داره.
- **Dispatcher مستقل از پلتفرم:** الگوی `BotClient` interface در `dispatcher.ts` این امکان را می‌دهد که منطق مکالمه از جزئیات تلگرام/بله جدا بماند، بدون نیاز به سرویس جداگانه.
- **دیپلوی ساده‌تر روی VPS:** یک Docker image، یک container اصلی (+ Postgres)، به‌جای هماهنگ‌سازی دو سرویس در Compose.

اگر در آینده حجم پروژه آن‌قدر رشد کرد که جدا کردن UI از بات ارزش داشت، این کار در هر زمانی با کمترین تغییر در لایه‌ی دیتابیس ممکن است — ولی برای شروع، پیچیدگی غیرلازم است.

---

## ۲. نقشه ماژول‌ها (Module Map)

> این ساختار مستقیماً بر اساس معماری پروژه‌ی تسویه‌یار طراحی شده — نه از صفر — چون آن معماری قبلاً در تولید (Railway، و قابل اجرا روی گوشی/Termux) تست شده است.

```
project-root/                    # یک پروژه‌ی Next.js واحد
├── app/
│   ├── api/
│   │   └── bot/
│   │       └── webhook/
│   │           └── route.ts     # نقطه ورود Webhook تلگرام؛ فقط parse + صدا زدن dispatcher
│   ├── transactions/            # صفحه‌ی مدیریتی: لیست/ویرایش تراکنش‌ها
│   ├── inventory/               # صفحه‌ی مدیریتی: موجودی انبار (فاز ۲)
│   ├── reports/                 # صفحه‌ی مدیریتی: گزارش‌ها (فاز ۳)
│   └── login/                   # ورود ادمین به پنل (طبق الگوی lib/auth.ts پروژه‌ی قبلی)
├── lib/
│   ├── bot/
│   │   ├── telegramApi.ts       # کلاینت HTTP خام تلگرام (کپی/تطبیق از تسویه‌یار)
│   │   ├── dispatcher.ts        # state machine مکالمه، مستقل از پلتفرم (BotClient interface)
│   │   └── receiptStorage.ts    # ذخیره‌ی عکس‌های ارسالی (باتری/کارت گارانتی)
│   ├── stt/                     # قرارداد صوت → متن (فراخوانی providers/stt/*)
│   ├── vision/                  # قرارداد عکس+متن → JSON (فراخوانی providers/vision/*)
│   ├── providers/
│   │   ├── stt/                 # groq.ts, openrouter.ts, ...
│   │   └── vision/              # gemini.ts, openrouter.ts, ...
│   ├── parser/                  # اعتبارسنجی و نرمال‌سازی JSON خروجی AI
│   ├── inventory/               # منطق کسر/افزایش موجودی (فاز ۲)
│   ├── reports/                 # گزارش‌گیری و Cron Jobها (فاز ۳)
│   ├── dateParsing.ts           # پارس تاریخ فارسی/شمسی (کپی/تطبیق از تسویه‌یار)
│   ├── prisma.ts                # کلاینت Prisma (نقطه‌ی واحد دسترسی به دیتابیس)
│   └── logger.ts                # پیکربندی مرکزی لاگ
├── prisma/
│   └── schema.prisma
├── scripts/
│   └── start.sh                 # منطق بوت (migrate deploy، نه db push خام در production)
├── Dockerfile                   # چندمرحله‌ای، طبق الگوی تسویه‌یار
├── docker-compose.yml           # app + db + nginx
├── nginx/                       # ریورس‌پراکسی + SSL
├── ROADMAP.md
└── ARCHITECTURE.md
```

**نکته مهم:** برخلاف پیش‌نویس اول این سند، دیگر دو سرویس جدا (بات و UI) نداریم. همه در یک پروژه‌ی Next.js هستند؛ Webhook فقط یک API Route در همان پروژه است. صفحات `app/` مستقیماً به `lib/prisma.ts` دسترسی دارند؛ Webhook route فقط `dispatcher.ts` را صدا می‌زند و خودش منطق ندارد.

**قانون طلایی:** هیچ ماژولی مستقیم به جزئیات داخلی ماژول دیگر دسترسی ندارد — فقط از طریق قرارداد ورودی/خروجی زیر با هم حرف می‌زنند.

---

## ۳. قرارداد هر ماژول (Input/Output Contract)

### `lib/bot/dispatcher.ts` — منطق مکالمه (مستقل از پلتفرم)
- **ورودی:** پیام نرمال‌شده (متن/عکس/صوت) از طریق `BotClient` interface — نه مستقیم آبجکت خام تلگرام
- **خروجی:** فراخوانی ماژول‌های دیگر با داده‌ی تمیزشده؛ دستور ارسال پیام/دکمه به کاربر از طریق همان `BotClient`
- **مسئولیت:** فقط state machine مکالمه. هیچ منطق پردازش AI یا کوئری مستقیم دیتابیس اینجا نباشد — این کار وظیفه‌ی `stt/`, `vision/`, `parser/`, `prisma.ts` است.
- **چرا مستقل از پلتفرم:** اگر بعداً بله یا واتس‌اپ هم اضافه شد، فقط یک `BotClient` جدید پیاده می‌شود؛ `dispatcher.ts` دست نمی‌خورد (دقیقاً الگوی تسویه‌یار).

### `lib/bot/telegramApi.ts` — لایه HTTP خام تلگرام
- **ورودی:** توکن بات + متد API (sendMessage، getFile، و غیره)
- **خروجی:** پاسخ خام تلگرام یا خطای شبکه
- **مسئولیت:** فقط ارتباط HTTP. هیچ منطق تصمیم‌گیری اینجا نیست.

### `stt/`
- **ورودی:** `{ file_id: string, request_id: string }`
- **خروجی موفق:** `{ success: true, text: string }`
- **خروجی خطا:** `{ success: false, error: string }`
- **قانون:** تایم‌اوت ۵ ثانیه‌ای؛ در خطا هرگز throw نکند، همیشه آبجکت برگرداند.

### `vision/`
- **ورودی:** `{ photo_file_id: string | null, transcript: string, request_id: string }`
- **خروجی موفق:**
```json
{
  "success": true,
  "data": {
    "action": "SELL",
    "product_name": "string",
    "serial_number": "string | null",
    "customer_name": "string | null",
    "customer_mobile": "string | null",
    "total_price": "number | null",
    "paid_amount": "number | null",
    "payment_type": "نقد | چک | نسیه | null",
    "due_date": "ISO date | null",
    "custom_fields": { "...": "..." }
  }
}
```
- **خروجی خطا:** `{ success: false, error: string }`
- **قانون:** هرگز حدس اضافه نزند؛ فیلد نامشخص = `null`، نه مقدار ساختگی.

### `parser/`
- **ورودی:** خروجی خام `vision/`
- **خروجی:** همان ساختار، پس از اعتبارسنجی نوع داده (مثلاً `total_price` باید عدد باشد) و نرمال‌سازی (شماره موبایل، تاریخ)
- **قانون:** اگر فیلد اجباری (`action`, `total_price`) غایب یا نامعتبر بود، به `bot/` علامت بدهد که از کاربر بخواهد اصلاح کند — خودش تصمیم نگیرد.

### `lib/prisma.ts` — لایه دیتابیس
- **ورودی:** JSON نرمال‌شده از `parser/` + نوع عملیات (insert/update/query) از طریق توابع Prisma
- **خروجی:** رکورد ذخیره‌شده یا خطای Prisma
- **قانون:** تمام کوئری‌ها از طریق Prisma Client انجام می‌شود؛ هیچ SQL خامی بیرون از موارد کاملاً ضروری نوشته نشود. `schema.prisma` تنها منبع حقیقت ساختار داده است.

### `inventory/` (فاز ۲)
- **ورودی:** تراکنش تاییدشده (خرید یا فروش)
- **خروجی:** موجودی به‌روزشده یا خطای «موجودی کافی نیست»
- **قانون:** هرگز موجودی منفی ثبت نشود؛ اگر فروش بیشتر از موجودی بود، به کاربر هشدار بده و تایید دستی بخواه.

### `reports/` (فاز ۳)
- **ورودی:** بازه زمانی یا trigger زمان‌بندی‌شده
- **خروجی:** متن گزارش آماده ارسال به تلگرام

### `logger.ts`
- در بخش ۵ توضیح کامل داده شده.

---

## ۴. جریان کامل یک درخواست (End-to-End Flow)

```
کاربر عکس+صوت می‌فرستد
        │
        ▼
   [app/api/bot/webhook] درخواست را می‌گیرد، request_id می‌سازد، لاگ می‌زند
        │
        ▼
   [lib/bot/dispatcher] تشخیص وضعیت مکالمه، صدا زدن مراحل بعدی
        │
        ▼
   [lib/stt/] صوت → متن خام   ──(خطا)──► پیام Fallback به کاربر
        │ موفق
        ▼
   [lib/vision/] عکس+متن → JSON خام   ──(خطا)──► پیام Fallback به کاربر
        │ موفق
        ▼
   [lib/parser/] اعتبارسنجی و نرمال‌سازی   ──(فیلد ناقص)──► درخواست اصلاح از کاربر
        │ معتبر
        ▼
   [dispatcher] نمایش خلاصه + دکمه تایید/اصلاح (از طریق telegramApi.ts)
        │ کاربر تایید کرد
        ▼
   [lib/inventory/] (اگر فاز ۲ فعال) بررسی/به‌روزرسانی موجودی
        │
        ▼
   [lib/prisma.ts] ذخیره نهایی تراکنش
        │
        ▼
   [dispatcher] پیام تاییدیه نهایی به کاربر + لاگ پایان با request_id
```

هر پیکان = یک نقطه‌ی دقیق برای گذاشتن لاگ. اگر جایی خطا رخ داد، از لاگ مشخص است دقیقاً کدام مرحله شکسته. همین مسیر از صفحات مدیریتی (`app/transactions`, `app/inventory`) هم قابل مشاهده و ویرایش است — چون همه به یک `prisma.ts` وصل‌اند.

---

## ۵. استاندارد لاگ‌محور بودن

هر لاگ باید این شکل باشد (JSON تک‌خطی، برای پارس راحت با `docker logs`):

```json
{
  "level": "INFO | WARN | ERROR",
  "request_id": "uuid یکتا برای هر پیام ورودی",
  "module": "webhook | dispatcher | stt | vision | parser | prisma | inventory",
  "step": "start | success | fallback | error",
  "message": "توضیح کوتاه انسانی",
  "meta": { "هر داده اضافه‌ی مرتبط" },
  "timestamp": "ISO 8601"
}
```

**قوانین:**
- `request_id` باید از لحظه‌ی ورود پیام تا لحظه‌ی پایان (ذخیره یا خطا) در همه‌ی لاگ‌های آن مسیر یکسان بماند.
- `ERROR` همیشه باید `stack trace` کامل در `meta` داشته باشد.
- `WARN` برای همه‌ی موارد Fallback (یعنی وقتی سیستم خودش را نجات داد ولی چیزی ایده‌آل نبود).
- هرگز داده‌ی حساس کامل (مثل شماره موبایل کامل مشتری) در لاگ INFO سطح بالا چاپ نشود؛ اگر لازم بود، فقط چند رقم آخر.

**چرا این مهم‌تر از خود کد است:** روی VPS دسترسی به دیباگر گرافیکی نداری. تنها راهت این است که وقتی یک تراکنش خراب شد، لاگ‌های همان `request_id` را با `docker compose logs app | grep request_id` فیلتر کنی و دقیقاً ببینی کجا شکست — بدون اینکه لازم باشد کل کد را بازخوانی کنی یا دوباره تست کنی.

---

## ۶. اصول کلی که همیشه رعایت می‌شود

1. **هیچ ماژولی throw نکند بدون گرفتن.** همیشه `{ success, ... }` برگردانده شود تا لایه بالاتر تصمیم بگیرد.
2. **هر خروجی AI قبل از ذخیره، اعتبارسنجی می‌شود** — هرگز مستقیم از `vision/` به `db/` نرود.
3. **عملیات حساس (کم/زیاد شدن موجودی، ثبت تراکنش) همیشه نیاز به تایید صریح کاربر دارد**، حتی اگر AI با اطمینان بالا تشخیص داده باشد.
4. **فیلدهای داینامیک همیشه در `custom_fields`/`custom_data` می‌روند** — هیچ‌وقت برای اضافه کردن یک فیلد جدید کد ماژول‌ها تغییر نمی‌کند، فقط پرامپت Vision و در صورت نیاز نمایش در `dispatcher.ts` یا صفحات مدیریتی.
5. **هر فاز جدید فقط بعد از عبور از «معیار عبور» فاز قبلی در `ROADMAP.md` شروع می‌شود.**
6. **استفاده مجدد از الگوی تسویه‌یار، نه کپی کورکورانه:** فایل‌هایی مثل `telegramApi.ts` و `dispatcher.ts` به‌عنوان الگوی معماری استفاده می‌شوند و برای این پروژه (schema ساده‌تر، بدون تطبیق بدهی) بازنویسی/ساده می‌شوند — نه اینکه عیناً و بدون بازبینی کپی شوند.

---

## یادداشت سوالات باز (برای بحث قبل از فاز ۰)

- [x] ~~Webhook یا Polling~~ → تصمیم: Webhook پشت Nginx روی همان دامنه‌ی UI مدیریتی
- [x] ~~Railway یا جای دیگر~~ → تصمیم: VPS شخصی + Docker Compose
- [ ] دامنه/ساب‌دامنه برای Webhook و UI مدیریتی از کجا تامین می‌شود؟ (لازم برای HTTPS و Let's Encrypt)
- [ ] مشخصات VPS (RAM/CPU) — برای اجرای همزمان بات + Postgres + Next.js UI + Nginx حداقل ۲ گیگ RAM پیشنهاد می‌شود
- [ ] فرمت دقیق پیام تاییدیه در تلگرام (چه فیلدهایی نمایش داده شود، ترتیب چطور باشد)
- [ ] سقف رایگان Groq/Gemini دقیقاً چقدر است و با حجم تراکنش روزانه‌ی تو کفایت می‌کند؟
- [ ] آیا UI مدیریتی از فاز ۰ لازم است یا می‌تواند به فاز بعدتر موکول شود؟ (پیشنهاد: اسکلت UI از فاز ۰ باشد، ولی صفحات واقعی بعد از فاز ۱)
