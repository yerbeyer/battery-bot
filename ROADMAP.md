# نقشه راه پروژه — بات تلگرامی حسابداری/انبار باتری‌فروشی

> این فایل مرجع وضعیت پروژه است. هر بخش که کامل شد، وضعیتش را از ⏳ به 🔨 و بعد به ✅ تغییر بده.
> هدف: هر لحظه با یک نگاه به این فایل بفهمی الان کجای پروژه‌ای و فاز بعدی چیست.

**وضعیت‌ها:**
- ✅ تکمیل‌شده و تست‌شده
- 🔨 در دست ساخت
- ⏳ برنامه‌ریزی‌شده (هنوز شروع نشده)
- ❌ لغو‌شده / کنار گذاشته‌شده

---

## فاز ۰ — ستون فقرات (Skeleton) 🔨

هدف: یک بات تلگرامی زنده که پیام می‌گیرد، لاگ می‌زند، و جواب echo می‌دهد. بدون AI، بدون دیتابیس واقعی.

- [x] راه‌اندازی پروژه‌ی Next.js واحد (App Router) طبق ARCHITECTURE.md — نه دو سرویس جدا
- [x] راه‌اندازی Prisma + `schema.prisma` اولیه (`BotSession` + `LogEntry`)
- [x] `Dockerfile` چندمرحله‌ای (طبق الگوی تسویه‌یار، بدون Python/WeasyPrint فعلاً) + `docker-compose.yml` (app + db)
- [x] راه‌اندازی نمونه‌ی Nginx (`nginx/battery-bot.conf.example`) — نصب واقعی روی VPS با دامنه‌ی خودت باقی مانده
- [x] پیاده‌سازی `lib/bot/telegramApi.ts` (بر اساس الگوی تسویه‌یار) و اتصال بات به تلگرام از طریق **Webhook** در `app/api/bot/webhook/route.ts`
- [x] پیاده‌سازی `lib/bot/dispatcher.ts` با `BotClient` interface (echo + `/start`, `/cancel`, `/ping`)
- [x] سیستم لاگ‌محور پایه در `lib/logger.ts` (هر پیام یک `request_id` می‌گیرد، خروجی stdout + جدول `LogEntry`)
- [x] صفحه‌ی health-check در `app/page.tsx` + `app/api/health/route.ts`
- [x] فایل `.env.example` با همه‌ی متغیرهای لازم (شامل placeholder فاز ۱)
- [x] `scripts/set-webhook.ts` برای ثبت وب‌هوک نزد تلگرام
- [x] Lint و type-check تمیز (`npx eslint .` و `npx tsc --noEmit` بدون خطا)
- [x] راهنمای Railway (`RAILWAY.md`) — مستخرج از تجربه‌ی واقعی دیپلوی پروژه‌ی قبلی
- [ ] **باقی‌مانده (نیاز به محیط واقعی تو):** `npx prisma generate` روی ماشین خودت (در سندباکس من به binaries.prisma.sh دسترسی نبود)
- [ ] **باقی‌مانده:** انتخاب مسیر میزبانی — VPS شخصی (`README.md`) یا Railway (`RAILWAY.md`) — و اجرای واقعی طبق همان راهنما
- [ ] **باقی‌مانده (فقط اگر VPS):** راه‌اندازی واقعی Nginx+SSL با دامنه‌ی خودت
- [ ] **باقی‌مانده:** ساخت بات در @BotFather، گرفتن توکن، ثبت وب‌هوک
- [ ] **باقی‌مانده:** تست دستی end-to-end (پیام بفرستی، جواب بگیری، لاگ را در docker logs / Railway Deployments ببینی)

**معیار عبور به فاز بعد:** پروژه روی VPS از طریق `docker compose up` بالا بیاید، Webhook تلگرام با HTTPS کار کند، پیام متنی بفرستی و جواب بگیری، و با `docker compose logs` کل مسیر پیام از روی `request_id` قابل ردیابی باشد.

> کدِ فاز ۰ کامل نوشته و lint/type-check شده تحویل داده شد. آنچه باقی مانده صرفاً اجرای عملیاتی روی محیط واقعی توست (VPS، دامنه، توکن بات) — چیزی که از سندباکس من قابل انجام نیست.

---

## فاز ۱ — ثبت فروش ساده (MVP هسته‌ای) ⏳

هدف: فروشنده عکس + صوت می‌فرستد → تراکنش فروش با تایید کاربر در دیتابیس ذخیره می‌شود. **بدون انبار، بدون گزارش.**

- [ ] ماژول STT (Groq Whisper) — تبدیل صوت به متن خام
- [ ] ماژول Vision (Gemini Flash) — خواندن عکس + استخراج JSON ساختاریافته از متن پیاده‌شده
- [ ] طراحی و ساخت جدول `transactions` (با ستون `custom_data JSONB`)
- [ ] مرحله‌ی تاییدیه: نمایش JSON استخراج‌شده به‌صورت خوانا با دکمه‌های «تایید ✅» / «اصلاح ✏️»
- [ ] ذخیره نهایی در PostgreSQL پس از تایید
- [ ] مکانیزم Fallback: اگر STT یا Vision خطا داد یا تایم‌اوت شد → پیام «ثبت دستی» با فرمت متنی ساده (ریپلای با `نام - مبلغ - سریال`)
- [ ] تست دستی: حداقل ۱۰ سناریوی واقعی (صوت واضح، صوت نامفهوم، عکس تار، بدون موبایل، و...)

**معیار عبور به فاز بعد:** بتوانی یک فروش واقعی را از عکس+صوت تا ذخیره در دیتابیس، با نرخ خطای قابل قبول، انجام دهی.

---

## فاز ۲ — انبار و ثبت خرید ⏳

هدف: اضافه کردن ماژول انبار؛ فروش‌ها موجودی را کم می‌کنند، خریدها اضافه می‌کنند.

- [ ] طراحی و ساخت جدول `inventory`
- [ ] تشخیص نوع عملیات (خرید/فروش) از روی صوت — با تاییدیه اجباری از کاربر (چون ریسک بالاست)
- [ ] منطق کسر/افزایش موجودی هنگام تایید تراکنش
- [ ] اتصال `transactions.product_id` به `inventory`
- [ ] هشدار موجودی کم (مثلاً وقتی یک مدل باتری کمتر از N عدد شد)
- [ ] تست: چند سناریوی خرید و فروش پشت‌سرهم، بررسی صحت موجودی نهایی

**معیار عبور به فاز بعد:** موجودی انبار بعد از ۲۰-۳۰ تراکنش آزمایشی، با شمارش دستی مطابقت داشته باشد.

---

## فاز ۳ — گزارش‌گیری و یادآورها ⏳

هدف: خروجی‌های مدیریتی و اعلان‌های خودکار.

- [ ] دستور/ویس «گزارش فروش امروز» / «گزارش هفته»
- [ ] Cron Job روزانه: بررسی چک‌های سررسید و ارسال یادآور
- [ ] Cron Job شبانه: خلاصه فروش روز + موجودی نقد/چک + وضعیت انبار
- [ ] (اختیاری) فرمت گزارش خروجی (متن ساده در تلگرام کافی است؛ اگر PDF فارسی خواستی، الگوی `persianPdf.ts`/WeasyPrint از تسویه‌یار آماده و قابل استفاده مجدد است — کار اضافه‌ی کمی می‌برد)

**معیار عبور به فاز بعد:** گزارش‌ها به‌صورت خودکار و دقیق در ساعت مقرر ارسال شوند.

---

## فاز ۴ — پرداخت تکمیلی و بهبودها (Backlog) ⏳

موارد غیر ضروری برای MVP که بعداً بررسی می‌شوند:

- [ ] Telegram Mini App برای ورود دستی گرافیکی (به‌جای فرمت متنی ریپلای)
- [ ] پنل وب گزارش‌گیری (اگر لازم شد)
- [ ] پشتیبان‌گیری خودکار از دیتابیس
- [ ] مدیریت چند فروشگاه / چند کاربر (اگر قرار شد به کسب‌وکار دیگری هم بدهی)

---

## یادداشت تصمیمات مهم (Decision Log)

هر بار که یک تصمیم معماری مهم گرفتی، اینجا یک خط اضافه کن — تا بعداً یادت نرود چرا آن‌طور تصمیم گرفتی.

| تاریخ | تصمیم | دلیل |
|---|---|---|
| ۱۴۰۵/۰۵/۱۵ | میزبانی: VPS شخصی + Docker Compose به‌جای Railway | کنترل کامل، بدون قفل‌شدن به یک PaaS خاص، قابل مهاجرت آسان |
| ۱۴۰۵/۰۵/۱۵ | ارتباط تلگرام: Webhook به‌جای Polling | پایدارتر برای پروداکشن، هماهنگ با دامنه‌ی UI مدیریتی |
| ۱۴۰۵/۰۵/۱۶ | معماری: یک پروژه‌ی Next.js واحد (نه دو سرویس جدا bot+admin-ui) | بعد از بررسی پروژه‌ی تسویه‌یار؛ کد مشترک کمتر تکراری، الگوی تست‌شده در تولید (جزئیات در ARCHITECTURE.md بخش ۱.۲) |
| ۱۴۰۵/۰۵/۱۶ | استفاده از Prisma (نه کوئری خام SQL) | همان ابزار آشنا از پروژه‌ی قبلی؛ schema واحد و type-safe |
| ۱۴۰۵/۰۵/۱۶ | الگوبرداری از `dispatcher.ts`/`telegramApi.ts` تسویه‌یار برای لایه بات | الگوی BotClient مستقل از پلتفرم، از قبل تست‌شده در تولید |
| ۱۴۰۵/۰۵/۱۵ | لایه Provider Adapter برای STT/Vision | امکان جابجایی بین Groq/Gemini و 9router/OpenRouter بدون تغییر کد اصلی |
| ۱۴۰۵/۰۵/۱۷ | Railway به‌عنوان گزینه‌ی جایگزین VPS (نه یا/یا، بلکه هردو) اضافه شد | همان `Dockerfile` را بدون تغییر قبول می‌کند؛ راهنمای Railway از تجربه‌ی واقعی پروژه‌ی قبلی استخراج و در `RAILWAY.md` مستند شد |
